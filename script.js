let total = 0;
let firstNumber;
let operator;
let secondNumber;

// Basic Functions

// Add
function add(num) {
    total += num;
    return total;
}

// Subtract
function subtract(num) {
    total -= num;
    return total;
}

// Multiply
function multiply(num) {
    total *= num;
    return total;
}

// Divide
function divide(num) {
    total /= num;
    return total;
}

// Operate
function operate(operator, firstNumber, secondNumber) {
    
}